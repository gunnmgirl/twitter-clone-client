const INITIAL_STATE = {
  content: "",
  upvotes: 0,
  downvotes: 0,
  //loading: false,
  //error: false,
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
