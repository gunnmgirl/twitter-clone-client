const INITIAL_STATE = {
  name: "",
  email: "",
  posts: [],
  //loading: false,
  //error: false,
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
